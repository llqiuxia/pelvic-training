# AGENTS.md 指令

1. 使用中文回复

## Agent skills

### Issue tracker

该仓库的 issue 使用 GitHub Issues 追踪。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用默认的五分类标签。详见 `docs/agents/triage-labels.md`。

### Domain docs

单上下文模式（single-context）。详见 `docs/agents/domain.md`。
